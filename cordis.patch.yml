# ============================================================================
# dsh-balance-bubble —— DSH bundle patch
# ============================================================================
# 插件的挂载声明。DSH 启动时按 bundle 层栈叠加各插件 patch，本文件把余额气泡
# 挂件插入 Web profile 的配置树。
#
# 安装：
#   dsh plugin --profile web add dsh-balance-bubble
# 本地开发：
#   dsh plugin --profile web add link:E:\deepseekdm\dsh-balance-bubble
# ============================================================================

- insert:
    - id: dsh-balance-bubble
      name: dsh-balance-bubble
